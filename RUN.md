# اجرای مغز کامل مگس در محیط دوبعدی

کل کانکتوم FlyWire FAFB v783 — **۱۳۹٬۲۵۵ نورون، ۱۵٬۰۹۱٬۹۸۳ اتصال، ۵۴٬۴۹۲٬۹۲۲ سیناپس، بدون حذف حتی یک نورون**.

## پیش‌نیاز: فقط numpy

```bash
python3 -m pip install numpy            # پایتون ۳٫۹ یا بالاتر
```

هیچ چیز دیگری لازم نیست. بدون GPU، بدون PyTorch، بدون Brian2.
(`pyarrow` فقط اگر بخواهی گراف را از دادهٔ خام دوباره بسازی لازم است.)

## اجرا

```bash
python3 flybrain/server.py
```

بعد مرورگر را باز کن روی <http://localhost:8000>

بارگذاری گراف حدود ۱۰ ثانیه طول می‌کشد. اگر پورت ۸۰۰۰ اشغال بود:

```bash
PORT=8080 python3 flybrain/server.py
```

## راهنمای سریع رابط کاربری

| بخش | کار |
|---|---|
| **بوم چپ** | هر ۱۳۹٬۲۵۵ نورون سر جای آناتومیک واقعی‌اش. هر نقطه‌ای که روشن می‌شود، یک اسپایک واقعی است. |
| **آرنای وسط** | مگس، اشیاء، و مسیری که طی کرده. با ابزارهای بالای آرنا روی زمین قند/تلخی/آب/ستون بگذار. |
| **نوار شبکیه** | روشنایی ۹۶ ستون آزیموتی، همان چیزی که به چشم‌ها تزریق می‌شود. |
| **ستون راست** | لغزنده‌های حسی و حرکتی + تحریک مستقیم هر جمعیت نورونی. |

### سه چیزی که حتماً امتحان کن

1. **تغذیه:** یک قطرهٔ قند درست زیر مگس بگذار ← نورون‌های حرکتی تغذیه شلیک می‌کنند، خرطوم دراز می‌شود، قطره کم‌کم مصرف می‌شود. حالا روی همان نقطه مادهٔ تلخ بگذار ← تغذیه **دقیقاً صفر** می‌شود. این بازتولید شکل ۳ مقالهٔ Nature است.

2. **جانبی‌سازی لمسی:** جمعیت `bristle_left` را روی ۱۲۰ هرتز تحریک کن ← نورون‌های نزولی سمت چپ بلندتر می‌شوند و مگس به راست می‌پیچد. با `bristle_right` دقیقاً برعکس.

3. **جاذب بدن قارچی:** لغزندهٔ «سازش شلیک» را روی **صفر** بگذار (یعنی پارامترهای دقیق مقالهٔ Shiu) و چند ثانیه صبر کن تا مگس به چیزی بخورد. سلول‌های کنیون روی ۷۰ هرتز قفل می‌شوند و **دیگر خاموش نمی‌شوند**. لغزنده را برگردان روی ۰٫۰۵ تا آرام شود.

## اگر خواستی گراف را از صفر بسازی

دادهٔ ساخته‌شده داخل همین بسته هست، پس لازم نیست. ولی اگر خواستی:

```bash
bash flybrain/fetch_data.sh                    # ~۱۵۸ مگابایت دادهٔ عمومی
python3 -m pip install pyarrow
python3 flybrain/build_graph.py                # ~۱۵ ثانیه
python3 flybrain/calibrate.py                  # استخراج خوانش حرکتی
```

## تست بدون محیط (بازتولید شکل ۱ مقاله)

```bash
python3 flybrain/brain.py full
```

## محتویات بسته

```
flybrain/       موتور LIF، ساخت گراف، کالیبراسیون، محیط، سرور، رابط کاربری
data/build/     گراف آمادهٔ اجرا (۱۲۲ مگابایت گراف کامل + نسخهٔ سریع)
docs/           گزارش تحقیق عمیق فارسی
flybrain/README.md   مستندات فنی انگلیسی
```

## لایسنس داده

دادهٔ کانکتوم FlyWire: **CC BY-NC 4.0** (Dorkenwald و همکاران ۲۰۲۴).
پارامترهای مدل و جدول اتصالات علامت‌دار: Shiu و همکاران ۲۰۲۴، کد با لایسنس MIT.
